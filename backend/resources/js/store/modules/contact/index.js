const state = {
  contacts: [],
}

const getters = {}

const mutations = {
  SET_CONTACTS (state, contacts) {
    state.contacts = contacts
  },
}

const actions = {
}

export default {
  state,
  getters,
  mutations,
  actions,
}
